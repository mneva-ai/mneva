# mneva progress log
# Format: [DATE] SESSION | FEATURE_ID | ACTION | RESULT
# Append only. Never edit historical entries.
# Entries before 2026-07-31 are backfilled from git history and docs/, not contemporaneous.

[2026-05-01] backfill | repo-init | Repo created (mneva-ai/mneva) | v0.0.1 placeholder
[2026-05-10] backfill | plan-1-foundation | M0-M4: CLI + local API, no LLM | v0.1.0 alpha released
[2026-05-17] backfill | v0.1.1 | gap fixes + dep hygiene + CI matrix | released
[2026-05-17] backfill | v0.1.2 | Obsidian vault read-write integration | released
[2026-05-18] backfill | v0.1.3 | `mneva distill` command | released
[2026-05-22] backfill | v0.2.0 | MCP server (6 tools) + uvx-first install + opt-in diagnose --share | released
[2026-05-24] backfill | v0.2.1 | `mneva upgrade` + fixed --version drift (version now sourced only from src/mneva/__init__.py) | released
[2026-05-24] backfill | v0.2.2 | Fixed README MCP config: `uvx mneva-mcp` -> `uvx --from mneva mneva-mcp` | released; LAST COMMIT before dormancy
[2026-05-25] backfill | user-research | Ran HN/Reddit research (00:50), merged package (13:36), non-technical integrated package (14:03) | 3 docs written to docs/, left UNCOMMITTED for 67 days
[2026-05-25] backfill | strategy-decision | DECIDED: non-technical users are the beachhead; engineer line demoted to future engine/power-user tier. Git/token/benchmark/CLI excluded from v0.3 | recorded in nontechnical-user-research-integrated-2026-05-25.json
[2026-05-25] backfill | outreach-batch-1 | Sent 5 cold Reddit DMs (ersinkiymaz / Any_Enthusiasm8346 / FounderArcs / Such_Grace / yanguoyu) | 0 REPLIES. Own success bar was >=2 replies + 3 pain quotes + 1 $5+ WTP. All missed. Copy sent was never recorded.
[2026-05-25..2026-07-31] backfill | (none) | 67 days dormant, zero commits | -

[2026-07-31] review | project-audit | Deep review via gstack /health + repo/external state check | Engineering 10.0/10 (mypy clean, ruff clean, 172 passed, CI all green, 0 TODO markers). Traction ~0: 1 star, 0 forks, 0 watchers, 0 issues ever. PyPI downloads UNKNOWN (pypistats 429 x3). Core finding: strategy decided 5/25 had zero implementation follow-through, while the shipped surface (14 CLI commands + MCP server) is 100% the audience that decision demoted.
[2026-07-31] review | strategy-recheck | User reconfirmed non-technical beachhead is still operative; confirmed outreach WAS sent and got 0 replies | Dissent recorded: 0/5 has no statistical power (cold-DM base rate 5-20%, expected replies <1). State is ZERO evidence, not negative evidence. Next step is to fix the validation instrument, not to build product.
[2026-07-31] review | p0b-repo-hygiene | Migrated google-generativeai (EOL) -> google-genai; rewrote provider to Client API and updated its tests; added a None-response test | 173 passed (+1), ruff clean, mypy clean, EOL FutureWarning gone
[2026-07-31] review | p0b-repo-hygiene | Created CLAUDE.md + .claude/feature_list.json + .claude/claude-progress.txt; committed the 3 research docs | Harness established; 118KB of strategy assets now under version control
[2026-07-31] review | traction-data | PyPI download stats retrieved on the 4th attempt (pypistats 429'd x3) | last_month=54, last_week=4, last_day=1. Corrects the earlier "traction ~0" reading to "very low but non-zero". Still pulling ~4/week 67 days after the last release. Cannot separate CI/mirror/bot traffic from real users, and 0 issues / 1 star / 0 forks argue against a reachable user base -- so this does NOT justify switching P0 to interviewing existing users. Re-evaluate if monthly downloads jump an order of magnitude.

[2026-07-31] review | competitive-analysis | Deep read of MemTensor/memmy-agent (GitHub API + memmy.bot/docs via gstack /browse) | DIRECT COMPETITOR, same positioning, far ahead. Created 2026-07-16 (15 days), 355 stars / 37 forks, pushed daily, TypeScript/MIT, backed by MemTensor (the MemOS memory research team). Has: desktop app + CLI/TUI + OpenAI-compatible API + desktop pet; automatic history import from Cursor/Claude Code/Codex/OpenCode/OpenClaw/Hermes with a "First Meeting Report"; a four-layer memory engine (L1 Trace / L2 Policy / L3 World Model / Skill) with episode merging, reward backpropagation, reflection scoring, MMR dedup, Dream consolidation; messaging channels (TG/Discord/WeChat/Feishu/DingTalk); managed Chromium. Signed Mac+Win builds, CN and international variants, Product Hunt, Discord, free signup credits. mneva's feature surface is a STRICT SUBSET except Obsidian. Caveat: this is a read of their repo and docs, NOT a benchmark -- engine quality unverified, star-growth composition unknown.
[2026-07-31] review | strategy-reversal | REVERSED the 2026-05-25 non-technical beachhead decision. New direction: Markdown/privacy/Obsidian engineer line | REASONING (do not re-litigate without reading this): memmy already shipped a better version of the 5/25 plan's P1 (make it installable by non-technical users) -- desktop app, free credits, one-click install, auto history scan. Meanwhile mneva's only five remaining differentiators (Markdown is source of truth; zero account/cloud; Obsidian two-way sync; scoped to the memory layer only; usable as a Python library) matter ONLY to engineers, Obsidian users, and privacy-sensitive developers -- exactly the group 5/25 demoted. New positioning: "Your AI memory is your own Markdown files. Follows the repo, goes into git, opens in Obsidian, reviewed in a PR. No account, no cloud." memmy structurally cannot copy this: its four-layer engine requires a structured DB, and retreating to plain Markdown would disarm it. IMPORTANT: this reversal is NOT "non-technical demand was falsified" -- the 0/5 outreach had no statistical power and that question still has zero evidence. It is a competitive-position call. v0.3 main line reverts to the engineer roadmap in docs/user-research-hn-reddit-2026-05-25.md sections 7-8.
[2026-07-31] review | index-blocker | Code review found a hard blocker for every v0.3 feature | Indexer._init_schema() (indexer.py:51) uses CREATE TABLE IF NOT EXISTS, so adding a column is a SILENT NO-OP on existing databases. Codebase-wide grep found no rebuild / reindex / ALTER TABLE / migration entry point. Also: search() filters in SQL before BM25 ranking (indexer.py:89-100), so new filterable fields must be real columns -- post-filtering would break top_k. Consequence: schema versioning + rebuild() + `mneva reindex` is a PREREQUISITE, not a follow-up. It also closes a real gap, since CLAUDE.md architecture rule 1 ("SQLite is rebuildable from Markdown") is currently not true in code.
[2026-07-31] review | p0-reposition | Rewrote CLAUDE.md ## Current Strategy, updated ## Common Mistakes (added the two Indexer traps), rewrote .claude/feature_list.json around the engineer roadmap, cancelled the non-technical entries | Harness now matches the 7/31 direction
[2026-07-31] review | p1-0-index-migration-path | Added PRAGMA user_version schema versioning (_SCHEMA_VERSION=1) + Indexer.rebuild() from store.iter_records() + `mneva reindex` command | 177 passed (+4), ruff clean, mypy clean. Regression guard EMPIRICALLY VERIFIED: stashed the new indexer.py and reran, old code asserted 1 == 2 (legacy DB silently kept its shape). Side effect worth knowing: opening a stale-version DB now DROPs and rebuilds, so index rows with no backing Markdown file are discarded. That is correct per architecture rule 1, and no real install can produce such a row (cli.py capture writes Markdown first, indexes second) -- but it broke tests/integration/test_concurrent_capture.py::test_wal_upgrade_from_legacy_journal_mode, whose fixture fabricated an index-only row. Fixed the fixture to also write the Markdown (what a real v0.1.x install would have) and strengthened the assertion to check the record is findable, not just counted. The IRON regression intent is preserved, not weakened.
[2026-07-31] review | p1-0-hardening | Advisor flagged a rebuild concurrency race; investigated and found the theory only partly held | Added BEGIN IMMEDIATE + rollback + double-checked user_version to rebuild(). EMPIRICAL FINDING: the theorized interleave (B's DROP landing mid-way through A's inserts) does NOT reproduce -- a 2-thread test passed 10/10 even with the lock removed, because SQLite's single-writer lock + busy_timeout already serializes both full rebuilds. What the transaction DOES buy is atomicity of the version stamp w.r.t. the data: verified discriminating (fails `assert 1 == 5` without it). Docstrings and the test docstring corrected to claim only what is proven. SEPARATE REAL BUG found by that same test: PRAGMA journal_mode=WAL ran BEFORE busy_timeout was set, and SQLite returns SQLITE_BUSY for journal-mode conversion WITHOUT invoking the busy handler -- so two mneva-mcp processes opening a fresh/pre-WAL DB simultaneously could get "database is locked" at startup. Fixed: busy_timeout first, WAL switch best-effort. The pre-existing concurrency test hid this by pre-opening the DB before racing. Also added `reindex` to the help-listing test and both READMEs. 179 passed, ruff clean, mypy clean.
